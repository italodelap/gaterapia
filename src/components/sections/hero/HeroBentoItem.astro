---
const { class: className, ...restOfProps } = Astro.props;
---

<article
  class={`${className} col-span-10 sm:col-span-5 rounded-xl border border-black/10 overflow-hidden`}
  {...restOfProps}
>
  <slot />
</article>
