---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a
  href={href}
  aria-labelledby={label}
  class="group grid place-items-center transition-all duration-500 px-3 py-3"
>
  <span id={label} class="hidden">{label}</span>
  <slot />
</a>
